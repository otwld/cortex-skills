interface:
  display_name: "Setup Project AI Files"
  short_description: "Set up project AI instruction files"
  default_prompt: "Use $setup-agent-instructions to create or audit project AI instruction files."
policy:
  allow_implicit_invocation: false
