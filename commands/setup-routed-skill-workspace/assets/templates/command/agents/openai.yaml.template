interface:
  display_name: "{{command_display_name}}"
  short_description: "{{command_short_description}}"
  default_prompt: "Use {{command_invocation}} to run this direct command skill."
policy:
  allow_implicit_invocation: false
