interface:
  display_name: "{{entry_display_name}}"
  short_description: "{{entry_short_description}}"
  default_prompt: "Use {{entry_invocation}} when routing work through the {{workspace_name}} skill workspace."
policy:
  allow_implicit_invocation: false
