interface:
  display_name: "Setup Routed Skill Workspace"
  short_description: "Create routed skill workspaces"
  default_prompt: "Use $setup-routed-skill-workspace to initialize an entry-named routed skill workspace or add routed modules and command atoms."
policy:
  allow_implicit_invocation: false
