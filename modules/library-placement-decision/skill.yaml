name: library-placement-decision
description: "Internal Cortex module applied when deciding where new code, shared abstractions, moved files, extracted logic, or public entry points belong inside a modular TypeScript workspace."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Code placement decision: new library, libraries/ path, libs/ path, moved file, extracted logic, shared abstraction, package, app/lib path, or import location decision"
    medium:
      - "Where code belongs, new package, libraries path, libs path, shared code, moved file, extracted logic, or import location changes"
    weak:
      - "Code placement"

relations:
  before: []
  with:
    - "nx-module-boundaries"
    - "public-api-design"
    - "naming-consistency"
  after:
    - "skill-evolution"
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
