name: implementation-plan
description: "Internal Cortex module applied when requirements are known and the user asks to draft a plan, full plan, phases, roadmap, migration path, or written execution sequence before edits."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Implementation planning: user asks to draft a plan, plan to change something, full plan, phases, roadmap, migration path, or execution sequence for known work"
    medium:
      - "Draft a plan, plan to, full plan, phases, roadmap, cross-boundary change, migration, risky refactor, or substantial implementation"
    weak:
      - "Plan drafting"

relations:
  before: []
  with:
    - "design-intake"
    - "grill-with-docs"
    - "issue-decomposition"
    - "workspace-state-guard"
    - "test-first-discipline"
    - "architecture-drift-detector"
    - "library-placement-decision"
    - "public-api-design"
    - "code-documentation"
  after:
    - "plan-execution"
  excludes: []
  replaces: []

uses: []

resources:
  references:
    - "shared/skill-quality-standard.md"
    - "shared/vertical-slices.md"
  scripts: []
  templates: []
  assets: []
