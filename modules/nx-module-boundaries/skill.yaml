name: nx-module-boundaries
description: "Internal Cortex module applied when working in an Nx workspace and adding, moving, or reviewing projects, tags, dependencies, or module-boundary rules."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Nx boundary decision: apps/, libs/, apps, libs, tags, project dependencies, node library, browser runtime boundaries, or module-boundary rules change"
    medium:
      - "Apps/, libs/, apps, libs, node library, project tags, dependency direction, runtime boundary, or module-boundary rule changes"
    weak:
      - "Nx boundary decision"

relations:
  before:
    - "library-placement-decision"
  with:
    - "public-api-design"
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
