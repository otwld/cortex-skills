name: jest-conventions
description: "Internal Cortex module applied when creating, modifying, or reviewing Jest configuration, Jest tests, setup files, custom matchers, test environments, or Jest mocks."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Jest evidence for jest-conventions: jest.config, Jest setup file, Jest mock, custom matcher, testEnvironment, __mocks__, or test file intended for Jest"
    medium:
      - "Jest config, setup, mocks, matchers, environments, fixtures, or assertions"
    weak:
      - "Jest surface"

relations:
  before: []
  with:
    - "typescript-code-style"
    - "code-documentation"
  after:
    - "skill-evolution"
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
