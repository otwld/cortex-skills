name: agent-delegation
description: "Internal Cortex module applied when independent tasks, investigations, or review passes can be delegated without shared state or sequential dependencies."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Delegation: independent investigations, parallel review passes, isolated implementation briefs, or sub-agent handoff with no shared mutable state"
    medium:
      - "Independent task, parallel investigation, review pass, implementation handoff, or sub-agent coordination"
    weak:
      - "Delegation request"

relations:
  before: []
  with:
    - "plan-execution"
    - "review-gate"
    - "systematic-debugging"
    - "completion-verification"
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references:
    - "code-reviewer-prompt.md"
    - "implementer-prompt.md"
    - "spec-reviewer-prompt.md"
  scripts: []
  templates: []
  assets: []
