name: nx-conventions
description: "Internal Cortex module applied when creating, modifying, or reviewing Nx workspace configuration, generators, targets, project metadata, inferred tasks, or project graph behavior."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Nx workspace configuration: nx.json, project.json, workspace targets, generators, executors, inferred tasks, or project graph behavior changes"
    medium:
      - "Nx config, workspace graph, project metadata, generate, generators, executors, inferred targets, or project targets change"
    weak:
      - "Nx workspace config"

relations:
  before: []
  with:
    - "nx-module-boundaries"
    - "library-placement-decision"
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
