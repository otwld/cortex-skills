name: plan-execution
description: "Internal Cortex module applied when executing a written implementation plan task-by-task with validation checkpoints and no remaining design decisions."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Plan execution: user asks to implement this plan, implement the plan, execute a written plan, or continue a decision-complete plan"
    medium:
      - "/goal, implement this plan, implement the plan, execute the plan, or plan exists with no design decision remaining"
    weak:
      - "Plan execution request"

relations:
  before:
    - "workspace-state-guard"
  with:
    - "agent-delegation"
    - "test-first-discipline"
    - "code-documentation"
    - "review-gate"
    - "completion-verification"
  after:
    - "branch-completion"
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
