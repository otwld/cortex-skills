name: "agent-delegation"
description: "Internal Cortex module applied when independent tasks, investigations, or review passes can be delegated without shared state or sequential dependencies."
activation: "routed"
visibility: "hidden"
status: "active"
routing:
  priority: 5
  facets:
    intents:
      - "delegate-independent-work"
      - "coordinate-parallel-review"
      - "scope-isolated-implementation-handoff"
    surfaces:
      - "workflow-execution"
      - "subagent-briefing"
      - "evidence-reconciliation"
    risks:
      - "shared-mutable-state-between-workers"
      - "overlapping-file-mutations"
      - "uncited-delegated-conclusions"
    artifacts:
      - "delegation-brief"
      - "worker-report"
      - "review-findings"
    repo:
      - "separable-files-or-behavioral-surfaces"
      - "validation-commands-can-be-named-per-task"
    request:
      - "asks for subagents, workers, parallel investigation, parallel review, or isolated implementation handoff"
      - "work can be split into bounded tasks whose outputs are independently verifiable"
lifecycle:
  activate: "lifecycle/activate.md"
  run: "lifecycle/run.md"
uses: []
resources:
  references:
    - "code-reviewer-prompt.md"
    - "implementer-prompt.md"
    - "spec-reviewer-prompt.md"
  scripts: []
  templates: []
  assets: []
