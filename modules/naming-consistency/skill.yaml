name: naming-consistency
description: "Internal Cortex module applied when creating or renaming files, classes, functions, exports, DTOs, contracts, events, commands, providers, factories, or reusable abstractions."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Naming decision: files, classes, functions, exports, DTOs, contracts, events, commands, providers, factories, or reusable abstractions are created or renamed"
    medium:
      - "Naming, names, rename, files, classes, functions, exports, DTOs, contracts, events, commands, or providers change"
    weak:
      - "Naming decision"

relations:
  before: []
  with:
    - "public-api-design"
    - "typescript-api-conventions"
    - "code-documentation"
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references:
    - "shared/domain-glossary.md"
  scripts: []
  templates: []
  assets: []
