name: "public-api-design"
description: "Internal Cortex module applied when adding or changing exports, shared contracts, DTOs, reusable abstractions, entry points, package boundaries, or import paths."
activation: "routed"
visibility: "hidden"
status: "active"
routing:
  priority: 5
  facets:
    intents:
      - "design supported public exports and entry points"
      - "shape shared contracts, DTOs, and reusable abstraction boundaries"
    surfaces:
      - "package entry points"
      - "public exports"
      - "shared DTOs and contracts"
      - "consumer import paths"
      - "SDK or library surface"
    risks:
      - "implementation detail exported as a convenience"
      - "public contract allows invalid state"
      - "consumer migration impact is hidden"
      - "entry point grows broader than the owning module supports"
    artifacts:
      - "index or barrel file"
      - "exported type, class, function, or constant"
      - "DTO or schema"
      - "SDK contract"
      - "package import path"
    repo:
      - "TypeScript workspace"
      - "package or library boundary"
    request:
      - "adds or changes exports, entry points, DTOs, schemas, or contracts"
      - "changes package import paths or consumer-facing abstractions"
      - "asks whether a symbol should be public"
lifecycle:
  activate: "lifecycle/activate.md"
  plan: "lifecycle/plan.md"
  review: "lifecycle/review.md"
uses: []
resources:
  references: []
  scripts: []
  templates: []
  assets: []
