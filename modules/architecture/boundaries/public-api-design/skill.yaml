name: public-api-design
description: "Internal Cortex module applied when adding or changing exports, shared contracts, DTOs, reusable abstractions, entry points, package boundaries, or import paths."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Public API design: exports, entry points, SDK contracts, DTOs, reusable abstractions, package boundaries, or consumer import paths change"
    medium:
      - "Public API, export, entry point, SDK, abstracts, contract, DTO, reusable abstraction, package boundary, or import path changes"
    weak:
      - "Public API change"

relations:
  before:
    - "library-placement-decision"
  with:
    - "naming-consistency"
    - "typescript-api-conventions"
    - "code-documentation"
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
