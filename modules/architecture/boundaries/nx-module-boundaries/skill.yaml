name: "nx-module-boundaries"
description: "Internal Cortex module applied when working in an Nx workspace and adding, moving, or reviewing projects, tags, dependencies, or module-boundary rules."
activation: "routed"
visibility: "hidden"
status: "active"
routing:
  priority: 5
  facets:
    intents:
      - "preserve Nx project ownership and dependency direction"
      - "design or review Nx tags and dependency constraints"
    surfaces:
      - "Nx project graph"
      - "apps and libs project layout"
      - "project tags and dependency constraints"
      - "module-boundary lint rules"
      - "Node and browser runtime boundaries"
    risks:
      - "tag added to grant access instead of describing responsibility"
      - "module-boundary rule relaxed to hide an ownership mismatch"
      - "runtime-specific dependency crosses into an incompatible project"
      - "internal project path imported instead of a supported entry point"
    artifacts:
      - "nx.json"
      - "project.json"
      - "workspace ESLint configuration"
      - "project tag"
      - "dependency constraint"
      - "Nx app or library"
    repo:
      - "Nx workspace"
      - "TypeScript monorepo"
    request:
      - "adds, moves, or reviews an Nx app or library"
      - "changes project tags, dependency constraints, or module-boundary lint rules"
      - "fixes or evaluates an Nx module-boundary violation"
      - "changes dependency direction between Node, browser, feature, domain, or utility projects"
lifecycle:
  activate: "lifecycle/activate.md"
  plan: "lifecycle/plan.md"
  review: "lifecycle/review.md"
uses: []
resources:
  references: []
  scripts: []
  templates: []
  assets: []
