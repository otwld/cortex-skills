# Legacy Extracted Patterns

These details came from the source project this skill was extracted from. They
are legacy-only notes, not active rules.

- Legacy only: `@cortex/ng-cdk/forms` exposed `formServiceProvide` for form service provider wiring.
- Legacy only: the source project required `inject()` for most touched Angular files.
- Legacy only: reactive forms were mandatory for all form UI.
