name: no-transitional-architecture
description: "Internal Cortex module always loaded to prevent compatibility shims, legacy adapters, temporary wrappers, useless implementation layers, migration leftovers, parallel implementations, placeholder code, stubs, and dead-code preservation unless explicit compatibility constraints require them."
activation: routed
visibility: hidden
status: active

routing:
  priority: 10
  signals:
    strong:
      - "Transitional architecture: compatibility layer, legacy adapter, migration shim, temporary wrapper, useless implementation layer, parallel implementation, fallback preserving old behavior, placeholder implementation, stub, or dead-code preservation"
    medium:
      - "Compatibility layer, legacy adapter, migration shim, temporary wrapper, implementation layer, parallel implementation, placeholder implementation, stub, or obsolete code"
    weak:
      - "Transitional architecture"

relations:
  before: []
  with: []
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
