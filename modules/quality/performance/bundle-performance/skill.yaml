name: bundle-performance
description: "Internal Cortex module applied when changes may affect bundle size, tree-shaking, side effects, UI dependencies, entry points, or broadly consumed runtime code."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Bundle performance: bundle size, tree-shaking, side effects, optional dependency isolation, entry point broadening, or runtime dependency added to shared UI"
    medium:
      - "Bundle size, tree-shaking, side effects, optional integration, entry point, or broadly consumed runtime dependency"
    weak:
      - "Bundle risk"

relations:
  before: []
  with:
    - "public-api-design"
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references: []
  scripts: []
  templates: []
  assets: []
