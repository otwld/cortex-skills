name: skill-evolution
description: "Internal Cortex module applied when routing metadata, module signals, repeated agent failure, reusable workflow patterns, durable Cortex conventions, or doctrine gaps need to evolve."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Skill evolution: routing metadata, router behavior, module signals, repeated agent failure, reusable workflow pattern, workspace conventions, durable Cortex convention, or doctrine gap changes"
    medium:
      - "Routing metadata, router, module signals, repeated agent failure, recurring workflow, workspace conventions, durable convention, or doctrine gap emerges"
    weak:
      - "Doctrine gap"

relations:
  before: []
  with: []
  after: []
  excludes: []
  replaces: []

uses: []

resources:
  references:
    - "shared/skill-quality-standard.md"
  scripts: []
  templates: []
  assets: []
