name: extraction-decision
description: "Internal Cortex module applied when repeated logic, DTOs, contracts, data-access patterns, UI composition, or orchestration suggest extracting a reusable abstraction."
activation: routed
visibility: hidden
status: active

routing:
  priority: 5
  signals:
    strong:
      - "Extraction decision evidence for extraction-decision: repeated logic, duplicated DTO shape, shared contract candidate, reusable data-access pattern, repeated UI composition, or orchestration copied across owners"
    medium:
      - "Repeated logic, duplicated shape, shared contract candidate, reusable data access, repeated UI composition, or copied orchestration"
    weak:
      - "Extraction decision"

relations:
  before:
    - "library-placement-decision"
    - "nx-module-boundaries"
  with:
    - "public-api-design"
    - "naming-consistency"
    - "bundle-performance"
    - "code-documentation"
  after:
    - "skill-evolution"
  excludes: []
  replaces: []

uses: []

resources:
  references:
    - "shared/architecture-deepening.md"
  scripts: []
  templates: []
  assets: []
