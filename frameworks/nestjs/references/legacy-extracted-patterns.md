# Legacy Extracted Patterns

These details came from the source project this skill was extracted from. They
are legacy-only notes, not active rules.

- Legacy only: reusable Nest helpers were expected in packages named `nest-sdk`.
- Legacy only: framework-agnostic helpers were expected in packages named `ts-*`.
- Legacy only: reusable Nest packages commonly used names beginning with `nest-`.
