# Legacy Extracted Patterns

These details came from the source project this skill was extracted from. They
are legacy-only notes, not active rules.

- Legacy only: `@cortex/nest-mongoose` exposed an `AbstractRepository` base and `MatchRule` query composition.
- Legacy only: aggregation helpers included `buildMatchStage`, `buildPipeline`, `buildFacet`, and `buildPaginatedFacet`.
- Legacy only: relation helpers included `buildRelationLookup` and `buildRelationsLookup`.
- Legacy only: ObjectId conversion helpers included `toObjectId` and `toObjectIds`.
- Legacy only: framework-agnostic types were moved to `@cortex/ts-sdk`.
