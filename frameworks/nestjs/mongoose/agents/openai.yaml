interface:
  display_name: "(otwld) NestJS Mongoose Conventions"
  short_description: "Keep NestJS Mongoose schemas, ObjectId handling, repositories, aggregations, and persistence contracts explicit."
  default_prompt: "Use $nestjs-mongoose-conventions when creating or reviewing NestJS Mongoose schemas, models, repositories, aggregation helpers, ObjectId handling, or persistence-facing DTO contracts."
