# Legacy Extracted Patterns

These details came from the source project this skill was extracted from. They
are legacy-only notes, not active rules.

- Legacy only: `@cortex/ng-storybook` exposed reusable MSW handler factories and seeded data helpers.
- Legacy only: helper names included `createFakerGenetics`, `createMswSearchGetHandler`, and `search(...)`.
- Legacy only: `@cortex/ts-storybook/vite` exposed `createStorybookNodeSourceLoaderPlugin`.
- Legacy only: documentation used `source-loader:` imports and `<Source>` blocks for small source mirrors.
- Legacy only: new stories required updates to `apps/documentation/.storybook/tsconfig.json`.
