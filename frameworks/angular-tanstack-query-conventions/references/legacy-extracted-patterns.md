# Legacy Extracted Patterns

These details came from the source project this skill was extracted from. They
are legacy-only notes, not active rules.

- Legacy only: `@cortex/ng-tanstack` provided `createQueryKey`, `Skippable<T>`, `runSkippableQuery`, `PaginationInfo`, `selectPaginatedResponse`, `coerceQueryOptions`, `coerceQueryInfiniteOptions`, and `strictMutationOptions`.
- Legacy only: mutation helpers required metadata for downstream invalidation behavior.
