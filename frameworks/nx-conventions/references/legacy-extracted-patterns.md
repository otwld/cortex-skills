# Legacy Extracted Patterns

These details came from the source project this skill was extracted from. They
are legacy-only notes, not active rules.

- Legacy only: generic TypeScript packages used names like `ts-*`.
- Legacy only: Nest-specific reusable packages used names like `nest-sdk` and `nest-*`.
- Legacy only: scaffolding guidance named a custom `nx-generate` workflow.
