# Adr Format

ADRs record decisions future agents should not re-litigate. Offer an ADR only when a
decision is hard to reverse, surprising without context, and the result of a real
tradeoff. Store ADRs in docs/adr with sequential filenames. Keep most ADRs to a title
and a short paragraph explaining context, decision, and reason.
