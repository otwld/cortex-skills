# Vertical Slices

A vertical slice is a thin complete path through the system. It should deliver one
narrow behavior end to end, be independently verifiable, and name blockers. Prefer
vertical slices over database-only, API-only, UI-only, or tests-later issues. Classify
slices as AFK-ready only when no design decisions remain.
