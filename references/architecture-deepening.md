# Architecture Deepening

Architecture deepening vocabulary: module, interface, implementation, depth, seam,
adapter, leverage, and locality. Use the deletion test to identify shallow modules. The
interface is the test surface. One adapter is hypothetical; two adapters justify a real
seam. Classify dependencies as in-process, local-substitutable, remote owned, or true
external.
