# Prototype Guidance

A prototype is throwaway code that answers one design question. State the question
first. Logic prototypes test state models with a small runnable shell and portable
logic. UI prototypes compare several structurally different variants. Use in-memory
state by default, capture the answer, then delete or absorb the prototype.
