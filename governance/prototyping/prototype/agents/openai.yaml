interface:
  display_name: "(otwld) Prototype"
  short_description: "Create clearly throwaway logic or UI prototypes that answer one design question quickly."
  default_prompt: "Use $prototype when building a throwaway prototype to answer a design question, compare UI variants, or test a state model before committing production code."
