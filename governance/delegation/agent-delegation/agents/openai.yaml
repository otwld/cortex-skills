interface:
  display_name: "(otwld) Agent Delegation"
  short_description: "Delegate independent work safely"
  default_prompt: "Use $agent-delegation when independent tasks, investigations, or reviews can be split without shared state."
