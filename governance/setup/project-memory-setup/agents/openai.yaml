interface:
  display_name: "(otwld) Project Memory Setup"
  short_description: "Set up or verify durable project-memory artifacts."
  default_prompt: "Use $project-memory-setup when setting up or verifying project-memory artifacts."
policy:
  allow_implicit_invocation: false
