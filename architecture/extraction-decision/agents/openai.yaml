interface:
  display_name: "(otwld) Extraction Decision"
  short_description: "Decide when and how to extract"
  default_prompt: "Use $extraction-decision when repeated code, contracts, or orchestration suggest a reusable abstraction."
