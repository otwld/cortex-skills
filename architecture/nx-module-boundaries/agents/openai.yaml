interface:
  display_name: "(otwld) Nx Module Boundaries"
  short_description: "Enforce Nx project dependencies"
  default_prompt: "Use $nx-module-boundaries when adding, moving, or reviewing Nx projects, tags, dependencies, or module-boundary rules."
